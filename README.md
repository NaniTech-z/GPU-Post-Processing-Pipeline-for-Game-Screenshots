Takes in an image as input and performs operations that change the brightness, contrast, and gamma contrast of it with hard coded values. Also, benchmarks these operations running on the CPU and GPU.
